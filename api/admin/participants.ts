import { requireIdentity, json } from '../_lib/auth.js'; import { ensureSchema, sql } from '../_lib/db.js';
export default async function handler(request: Request) { try { await requireIdentity(request,true); await ensureSchema(); const rows=await sql`SELECT u.id AS user_id, u.name, u.email, a.id, a.scores, a.responses, a.completed_at FROM neon_auth."user" u JOIN LATERAL (SELECT * FROM assessments WHERE user_id=u.id ORDER BY completed_at DESC LIMIT 1) a ON true ORDER BY a.completed_at DESC`; return json({participants:rows}); } catch(error) { return error instanceof Response ? error : json({error:'Unable to load participant data.'},500); } }
